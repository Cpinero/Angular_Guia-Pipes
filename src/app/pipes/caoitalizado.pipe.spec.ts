import { CapitalizadoPipe } from './capitalizado.pipe';

describe('CaoitalizadoPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizadoPipe();
    expect(pipe).toBeTruthy();
  });
});
